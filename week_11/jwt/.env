# port=4000 #what is this port number?
# #what is this file and why we use this generally?
# JWT_SECRET=replace_with_long_random_string
# JWT_EXPIRES_IN=15m
# # why we are creating the src?



PORT=4000
# Generate a long random secret once (do NOT commit to Git)
# In a terminal: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=rJWT_SECRET=mySuperSecretKey123!
JWT_EXPIRES_IN=15m

